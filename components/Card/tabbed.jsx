// @flow
import * as React from 'react';
// import Card from './index';
// import type { CardProps } from './index';

type Props = {
  // className?: string,
  // tabs: Array<{
  //   selected: boolean, // Render this tab and show it as active in the list of tabs
  //   text: string, // Name of the tab in the list of tabs
  //   card: React.Element<typeof Card>, // React Element contained
  // }>,
};

/**
 * Componente responsable solamente de desplegar el sidebar
 */
export default class TabbedCards extends React.PureComponent<Props, void> {
  render() {
    return (
      null
    );
  }
}
