// @flow
import Paginated from './paginated';
import Table from './pure';

import type { PureTableProps as TableProps, PureTableActions as TableActions } from './pure';

export default Table;
export {
  Table,
  Paginated,
};

export type {
  TableProps,
  TableActions,
};
