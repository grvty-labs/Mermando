// @flow
import * as React from 'react';
import autobind from 'autobind-decorator';

type Props = {

};

type State = {

};

export default class InvalidToken extends React.PureComponent<Props, State> {
  render() {
    return (
      <div />
    );
  }
}
