.form-section {
  > .legend {
    color: var(--light-grey);
    display: block;
    font-size: 0.875em;

    &:not(:empty) {
      margin-bottom: 0.875em;
    }
  }

  > .input-atom,
  > .checkbox {
    width: auto;

    &:not(:last-child) {
      margin-bottom: 1em;
    }
  }
}
