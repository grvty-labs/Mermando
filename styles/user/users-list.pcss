.users-list {
  display: flex;
  flex-direction: column;

  > div {
    display: inherit;

    &.selected {
      background-color: var(--white-smoke);
    }

    > .avatar {
      font-size: 1.333em;
    }

    > .data {
      display: inherit;
      flex-direction: column;
      padding: 0 0.875em;

      > .name {
        color: var(--nero);
        font-size: 0.875em;
        line-height: 1;
        margin-bottom: 3px;
      }

      .email {
        color: var(--light-grey);
        font-size: 0.625em;
        line-height: 1;
      }

      .spec {
        color: var(--grey-2);
        font-size: 0.625em;
        line-height: 1;
      }
    }

    > .actions {
      &:not(:empty) {
        margin-left: auto;
      }
    }

    &:not(:last-of-type) {
      margin-bottom: 0.875em;
    }
  }
}
