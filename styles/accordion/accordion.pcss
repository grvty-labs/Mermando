.accordion {
  > .header {
    align-items: center;
    display: flex;
    justify-content: space-between;
  }

  > .content {
    .panel {
      height: 0;
      min-height: 0;
      overflow: hidden;
      transition: min-height 0.2s ease-out;
    }
  }
}
