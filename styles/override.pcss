:--button {
  &.main,
  &.secondary,
  &.discrete {
    font-weight: 700;
  }

  &.main,
  &.link {
    text-transform: none;
  }
}

table {
  thead {
    text-transform: uppercase;

    th {
      font-weight: normal;
    }
  }

  tbody {
    .checkbox {
      font-size: 1em;
    }
  }
}
