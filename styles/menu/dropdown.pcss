.dropdown-wrapper {
  position: relative;

  > button {
    color: var(--topbar-menu-link-color, #333);
    font-size: 0.875rem;
    text-decoration: none;

    &.angled {
      padding-right: 1.5rem;
      position: relative;

      &::after {
        align-items: center;
        border-bottom: 2px solid #333;
        border-left: 2px solid #333;
        bottom: 0;
        content: " ";
        display: flex;
        height: 0.4375rem;
        justify-content: center;
        position: absolute;
        right: 0.25rem;
        top: 0;
        transform: rotate(-45deg);
        width: 0.4375rem;
      }
    }

    &:hover,
    &:--enter-action {
      + .dropdown {
        visibility: visible;
      }

      &.angled {
        &::after {
          border-color: var(--royal-blue);
        }
      }
    }
  }

  > .dropdown {
    background-color: var(--snow, #fafafa);
    box-shadow:
      0 12px 20px 0 rgba(26, 26, 26, 0.07),
      0 0 6px 2px rgba(0, 0, 0, 0.05);
    display: flex;
    flex-direction: column;
    min-width: 8rem;
    position: absolute;
    right: 0;
    top: 100%;
    visibility: hidden;

    > button,
    > div {
      &.title {
        align-items: center;
        display: flex;
        justify-content: space-between;
        padding: 1rem;

        > div {
          color: var(--nero);
          display: flex;
          flex: 1;
          font-size: 0.75rem;
          justify-content: center;
          text-align: center;

          &:first-of-type {
            justify-content: flex-start;
          }

          &:last-of-type {
            justify-content: flex-end;
          }

          > div,
          > span {
            color: inherit;
            font-size: inherit;
          }
        }
      }

      &:not(.title) {
        font-size: 0.75rem;
        margin: 0 0.375rem;
        padding: 0.5rem 0.25rem;
        text-align: left;
      }

      &:not(:last-of-type) {
        border-bottom: solid 1px var(--whisper-2, #e9e9e9);
      }
    }

    &:hover,
    &:--enter-action {
      visibility: visible;
    }
  }
}
